class People {
  final String name;
  final int followers;
  final String imagePath;
  final bool isStreaming;
  final bool isVerified;

  People(this.name, this.followers, this.imagePath, this.isStreaming, this.isVerified);
}
